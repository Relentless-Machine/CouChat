# Spring Boot application properties

# Server port (optional, default is 8080)
server.port=8121

# Logging configuration (optional, Spring Boot provides sensible defaults)
# logging.level.com.couchat=DEBUG

# Database Configuration (SQLite - primarily for local storage, connection string might be managed in code)
# spring.datasource.url=jdbc:sqlite:couchat_storage.db
# spring.datasource.driver-class-name=org.sqlite.JDBC
# spring.jpa.database-platform=org.hibernate.dialect.SQLiteDialect
# spring.jpa.hibernate.ddl-auto=update # Be cautious with this in production
