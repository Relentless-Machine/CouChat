// F:\Git\CouChat\couchat-frontend\electron-preload.js
// This file is intentionally left blank for now.
// You can use this script to expose Node.js/Electron APIs to your renderer process securely.
// For example:
// const { contextBridge, ipcRenderer } = require('electron');
//
// contextBridge.exposeInMainWorld('electronAPI', {
//   sendMessage: (channel, data) => ipcRenderer.send(channel, data),
//   onMessage: (channel, func) => ipcRenderer.on(channel, (event, ...args) => func(...args)),
// });

console.log('Electron preload script loaded.');

